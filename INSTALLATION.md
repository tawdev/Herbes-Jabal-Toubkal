# دليل التثبيت السريع

## خطوات التثبيت

### 1. استيراد قاعدة البيانات

**طريقة 1: باستخدام phpMyAdmin**
1. افتح phpMyAdmin: `http://localhost/phpmyadmin`
2. أنشئ قاعدة بيانات جديدة باسم `tawabil_db`
3. اختر قاعدة البيانات
4. اضغط على تبويب "Import"
5. اختر ملف `database.sql`
6. اضغط "Go"

**طريقة 2: باستخدام سطر الأوامر**
```bash
mysql -u root -p < database.sql
```

### 2. إعداد الاتصال بقاعدة البيانات

افتح ملف `config/database.php` وتأكد من:
```php
define('DB_HOST', 'localhost');
define('DB_USER', 'root');
define('DB_PASS', '');  // كلمة مرور MySQL الخاصة بك
define('DB_NAME', 'tawabil_db');
```

### 3. إنشاء المجلدات المطلوبة

```bash
mkdir -p images/products
mkdir -p images/recipes
mkdir -p images/slider
mkdir -p uploads
```

### 4. إعداد الصلاحيات (Linux/Mac)

```bash
chmod 755 images/products
chmod 755 images/recipes
chmod 755 images/slider
chmod 755 uploads
```

### 5. فتح الموقع

افتح المتصفح وانتقل إلى:
```
http://localhost/tawabil
```

## 🔐 تسجيل الدخول للمسؤول

- **الرابط:** `http://localhost/tawabil/admin/login.php`
- **اسم المستخدم:** `admin`
- **كلمة المرور:** `admin123`

> ⚠️ **مهم جداً:** قم بتغيير كلمة المرور فوراً بعد أول تسجيل دخول!

## 📸 إضافة الصور

### صور المنتجات
- ضع الصور في: `images/products/`
- الأسماء الموصى بها: `product_name.jpg`

### صور الوصفات
- ضع الصور في: `images/recipes/`
- الأسماء الموصى بها: `recipe_name.jpg`

### صور السلايدر
- ضع الصور في: `images/slider/`
- الأسماء الموصى بها: `slide1.jpg`, `slide2.jpg`, `slide3.jpg`
- الحجم الموصى به: 1920x500 بكسل

## ✅ التحقق من التثبيت

1. ✅ افتح الصفحة الرئيسية - يجب أن تظهر بدون أخطاء
2. ✅ جرب صفحة المتجر - يجب أن تظهر المنتجات
3. ✅ سجل دخول كمسؤول - يجب أن تعمل لوحة التحكم
4. ✅ أضف منتج تجريبي - يجب أن يظهر في المتجر

## 🐛 حل المشاكل الشائعة

### خطأ في الاتصال بقاعدة البيانات
- تأكد من أن MySQL يعمل
- تحقق من بيانات الاتصال في `config/database.php`
- تأكد من استيراد قاعدة البيانات

### الصور لا تظهر
- تأكد من وجود المجلدات
- تحقق من الصلاحيات (755)
- تأكد من مسار الصور في قاعدة البيانات

### خطأ 404
- تأكد من أن Apache يعمل
- تحقق من ملف `.htaccess`
- تأكد من تفعيل mod_rewrite

### مشاكل في الجلسات
- تأكد من أن PHP يدعم Sessions
- تحقق من صلاحيات مجلد temp

## 📞 الدعم

إذا واجهت أي مشاكل، تحقق من:
1. ملف `README.md` للوثائق الكاملة
2. سجلات الأخطاء في PHP
3. سجلات Apache/Nginx

---

**تم التثبيت بنجاح! 🎉**

